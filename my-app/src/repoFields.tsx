interface repoFields {
    reponame : string, 
    owner: string,
    version: string,
    releaseDate: string,
    unread: boolean,
    id: string,
    description: string,
} 

export default repoFields;
